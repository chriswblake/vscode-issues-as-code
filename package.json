{
  "name": "vscode-issues-as-code",
  "displayName": "Issues as Code",
  "description": "Synchronize GitHub issues to a local .issues/ folder",
  "version": "0.1.0",
  "engines": {
    "node": "^22.0.0",
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "issuesAsCode.pullNow",
        "title": "Issues as Code: Pull Now"
      },
      {
        "command": "issuesAsCode.pushNow",
        "title": "Issues as Code: Push Now"
      },
      {
        "command": "issuesAsCode.refresh",
        "title": "Issues as Code: Refresh All"
      },
      {
        "command": "issuesAsCode.addSyncTargetRepoIssues",
        "title": "Issues as Code: Add Sync Target - Open issues on this repository"
      },
      {
        "command": "issuesAsCode.addSyncTargetMyIssuesOnGitHub",
        "title": "Issues as Code: Add Sync Target - My open issues on GitHub"
      },
      {
        "command": "issuesAsCode.addSyncTargetAssignedRepoIssues",
        "title": "Issues as Code: Add Sync Target - My open issues on this repository"
      },
      {
        "command": "issuesAsCode.publishFile",
        "title": "Issues as Code: Publish to Remote"
      }
    ],
    "configuration": {
      "title": "Issues as Code",
      "properties": {
        "issuesAsCode.fileNaming": {
          "type": "string",
          "default": "{issue-num}-{issue-title}",
          "description": "Template for issue file names.",
          "scope": "resource"
        },
        "issuesAsCode.pushOnSaveDelay": {
          "type": "number",
          "default": 60,
          "description": "Seconds to wait after last save before pushing to GitHub.",
          "scope": "resource"
        },
        "issuesAsCode.syncTargets": {
          "type": "array",
          "default": [],
          "description": "List of sync targets. Each entry defines a local folder and one or more plugin configurations (e.g. gh-issues). When empty and a GitHub remote is detected, the extension automatically adds a default open-issues target to workspace settings.",
          "scope": "resource",
          "items": {
            "type": "object",
            "required": [
              "filesDir"
            ],
            "properties": {
              "filesDir": {
                "type": "string",
                "description": "Local folder path where synced files are stored. Must be relative to the current workspace folder; absolute paths are not allowed."
              },
              "naming": {
                "type": "string",
                "description": "Template for file names. Use tokens like {gh-issues.number} and {gh-issues.title}."
              },
              "readOnly": {
                "type": "boolean",
                "default": false,
                "description": "When true, files are updated from remote but local changes are never pushed. Local edits are overwritten on each pull. Files are made read-only on disk."
              },
              "gh-issues": {
                "type": "object",
                "description": "GitHub Issues plugin configuration.",
                "properties": {
                  "filters": {
                    "type": "object",
                    "description": "Filter criteria for GitHub Issues.",
                    "properties": {
                      "repository": {
                        "type": "string",
                        "description": "GitHub repository in 'owner/repo' format, e.g. 'my-org/my-repo'."
                      },
                      "state": {
                        "type": "string",
                        "enum": [
                          "open",
                          "closed"
                        ],
                        "description": "Issue state filter."
                      },
                      "assignee": {
                        "type": "string",
                        "description": "Filter by assignee login."
                      },
                      "author": {
                        "type": "string",
                        "description": "Filter by author login."
                      },
                      "label": {
                        "oneOf": [
                          {
                            "type": "string"
                          },
                          {
                            "type": "array",
                            "items": {
                              "type": "string"
                            }
                          }
                        ],
                        "description": "Filter by one or more labels."
                      },
                      "created_at": {
                        "type": "string",
                        "description": "Filter by creation date, e.g. '>2025-01-01'. Supports {today-Nd} tokens."
                      }
                    }
                  }
                }
              },
              "gh-projects": {
                "type": "object",
                "description": "GitHub Projects v2 plugin configuration.",
                "properties": {
                  "filters": {
                    "type": "object",
                    "description": "Filter criteria for GitHub Projects.",
                    "properties": {
                      "projectId": {
                        "type": "string",
                        "description": "GitHub Project ID."
                      },
                      "state": {
                        "type": "string",
                        "description": "Item state filter."
                      }
                    }
                  }
                }
              },
              "tick-tick": {
                "type": "object",
                "description": "TickTick plugin configuration (future integration).",
                "properties": {
                  "filters": {
                    "type": "object",
                    "description": "Filter criteria for TickTick tasks.",
                    "properties": {
                      "list": {
                        "type": "string",
                        "description": "TickTick list name."
                      },
                      "state": {
                        "type": "string",
                        "description": "Task state filter."
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "issuesAsCode.pullInterval": {
          "type": "number",
          "default": 30,
          "description": "Minutes between automatic pulls from GitHub.",
          "scope": "resource"
        },
        "issuesAsCode.syncStatePath": {
          "type": "string",
          "default": ".issues/sync-state.yml",
          "description": "Path to the local sync state file that tracks the last-synced timestamp per issue. Must be relative to the current workspace folder; absolute paths are not allowed. This file is machine-local and should be gitignored.",
          "scope": "resource"
        },
        "issuesAsCode.showSyncState": {
          "type": "boolean",
          "default": false,
          "description": "Show the sync state file in the VS Code Explorer. When false (default), the file is hidden via files.exclude.",
          "scope": "resource"
        },
        "issuesAsCode.showSyncIcons": {
          "type": "object",
          "description": "Controls which sync status icons are shown on issue files in the Explorer.",
          "scope": "resource",
          "properties": {
            "newIssue": {
              "type": "boolean",
              "default": true,
              "description": "Show 'A' badge on new issues not yet pushed to remote."
            },
            "modified": {
              "type": "boolean",
              "default": true,
              "description": "Show 'M' badge on locally modified issues not yet pushed to remote."
            },
            "synchronized": {
              "type": "boolean",
              "default": true,
              "description": "Show '✓' badge on issues whose local copy matches the remote."
            }
          },
          "default": {
            "newIssue": true,
            "modified": true,
            "synchronized": true
          }
        },
        "issuesAsCode.enable_experimental_projects": {
          "type": "boolean",
          "default": false,
          "description": "Enable experimental GitHub Projects v2 metadata sync. Requires reloading VS Code to take effect.",
          "scope": "resource"
        }
      }
    }
  },
  "dependencies": {
    "@octokit/rest": "^20.1.2",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.118.0",
    "@vscode/test-electron": "^2.5.2",
    "mocha": "^11.7.5",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTests.js",
    "test:unit": "mocha --ui tdd out/test/fileManager.test.js out/test/configManager.test.js out/test/syncManager.test.js out/test/syncStateManager.test.js out/test/issueDecorationProvider.test.js out/test/publishCodeLensProvider.test.js out/test/frontmatterCompletionProvider.test.js"
  }
}
